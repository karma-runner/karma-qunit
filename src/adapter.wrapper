(function(window) {

%CONTENT%

window.QUnit.config.autostart = false;

if (window.removeEventListener) {
  window.removeEventListener('load', window.QUnit.load, false);
} else {
  window.detachEvent('onload', window.QUnit.load);
}

if (window.__karma__.config.filter && window.__karma__.config.filter.length) {
  window.QUnit.config.filter = window.__karma__.config.filter[0].toLowerCase();
}

window.__karma__.start = createQUnitStartFn(window.__karma__);
})(window);
